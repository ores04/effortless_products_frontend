import { Typography, Box } from '@mui/material';

export default function DatasetsPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Datasets
      </Typography>
      <Typography variant="body1">
        Manage your datasets here.
      </Typography>
    </Box>
  );
}
