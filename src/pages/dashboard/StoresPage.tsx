import { Typography, Box } from '@mui/material';

export default function StoresPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Stores
      </Typography>
      <Typography variant="body1">
        View available stores here.
      </Typography>
    </Box>
  );
}
